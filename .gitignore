# Dependencies
node_modules/
app/node_modules/

# Build output
app/dist/
app/.vite/
app/tsconfig.tsbuildinfo
app/*.tsbuildinfo

# Editor / OS / Claude
.claude/
.vscode/
.idea/
.DS_Store
*.log

# Simulation outputs — keep only the latest report manually if desired
app/sim_results/sim_data_*.json
# Note: .md reports are kept by default for reference

# Research simulator outputs — raw JSONL is heavy (~2KB/game × 10k games),
# regenerate locally. Insights .md reports can be committed manually if desired.
app/sim_results/research/


# Local env
.env
.env.local
